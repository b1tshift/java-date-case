# java-date-case
Решение кейс-задачи №2. Java-приложение для расчета возраста, дней недели и отрисовки ASCII-графики с использованием Java Time API.

# Кейс-задача №2: Стилистическое преобразование чисел (Java)
Реализация консольного приложения для анализа даты рождения и графического вывода данных в формате электронного табло.

## 📌 Условие задачи

Разработать программу на языке Java, которая:
1. Запрашивает у пользователя день, месяц и год рождения.
2. Реализует функции для определения:
   - Дня недели, соответствующего дате.
   - Является ли год високосным.
   - Текущего возраста пользователя.
3. Выводит дату рождения в формате `дд мм гггг` в виде графических символов (звездочек `*`), имитирующих электронное табло.

## 🛠 Техническое описание решения

При разработке использовались следующие подходы:
* **Java Time API (`java.time`):** Использование современных классов `LocalDate` и `Period` вместо устаревших `Date` и `Calendar`. Это гарантирует точность расчетов и чистоту кода.
* **Матричная отрисовка:** Для реализации электронного табло использован двумерный массив шаблонов цифр. Это избавляет код от громоздких конструкций `if-else` и `switch-case`.
* **Локализация:** Название дня недели выводится на русском языке с использованием `java.util.Locale`.

## 🚀 Инструкция по запуску

Для компиляции и запуска приложения выполните команды в терминале:

```bash
javac DateAnalyzer.java
java DateAnalyzer
